#!/usr/bin/expect

set user [lrange $argv 0 0]
set ip [lindex $argv 1]

puts "User: $user"
puts "IP: $ip"

set timeout 60

spawn sudo ssh-copy-id $user@$ip

expect {
    "Are you sure you want to continue connecting" {
        send "yes\r"
        exp_continue
    }
    eof
}
